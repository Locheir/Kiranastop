services:
  nodeapp:
    container_name: kirana-stop
    build:
      context: .
      dockerfile: Dockerfile
    image: kirana-stop
    ports:
      - "3001:3001"
    env_file:
      - .env
    restart: always